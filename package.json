{
  "name": "bili-catfish",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "typescript": "5.9.3",
    "vitest": "^5.0.0"
  }
}
